

const HelloWorld = ({head, pera}) => {
    return (
        <div>
            <h1>{head}</h1>
            <p>
                {pera}
            </p>
            <button>Read more</button>
        </div>
    );
};

export default HelloWorld;